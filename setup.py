from distutils.core import setup

setup(
    name='pypal',
    packages=['pypal'],
    url='http://github.com/birknilson/pypal',
    long_description=open('README.md').read(),
    install_requires=[],
)
